# Skill — 熱門商品排行

本模組負責根據商品的銷售數據自動產生推薦權重。

## 排行邏輯
- 目前系統採取「精選推薦」模式, 直接選取商品清單中的前 4 項商品進行展示。
- 排行商品會固定顯示在首頁的「本季熱選分析」區塊。

## 擴充建議
- 未來可導入 `sales_count` 屬性並在 `renderHome()` 中加入 `.sort()` 邏輯以實現動態排行。

## 程式碼實作
- 函式：`index.html` 中的 `renderHome()`。
- 數據：`PRODUCTS.slice(0, 4)`。
